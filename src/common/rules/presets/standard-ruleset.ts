import { Ruleset } from '../ruleset';

export const StandardRuleset: Ruleset = {
  boardWidth: 3,
  boardHeight: 3,
  handSize: 5,
  handModifier: null,
  borderModifier: null,
  visibilityModifier: null,
  pickModifier: null,
  captureModifiers: [],
};
